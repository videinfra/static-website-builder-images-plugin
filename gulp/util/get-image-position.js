/**
 * Returns position for cropping (sharp `extract`)
 * 
 */

module.exports = function getImagePosition (bitmap, size, options = {}) {
    if (size.crop && options.position) {

    } else {
        return null;
    }
}